# This is a basic workflow to help you get started with Actions

name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-infra:
    uses: /infra-pipeline/.github/workflows/reusable-infra-pipeline.yml
